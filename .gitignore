# deps
node_modules/
.pnpm-store/

# next
web/.next/
web/out/
web/next-env.d.ts

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# env / secrets
.env
.env.local
contracts/.env

# misc
.DS_Store
*.log
.vercel
.env*
!.env.example
# Any *.env file (e.g. contracts/.reviewer.env) — these hold testnet keys.
# `.env*` only matches names STARTING with .env, so this catch-all is needed.
*.env

# JetBrains
.idea/
